#include<iostream>
#include<map>
#include<set>
#include<algorithm>
using namespace std;
int M,N;
map<string,int> products;
set<pair<int,string>> sales;
int main()
{
    // Make std::cin and std::cout faster
    ios_base::sync_with_stdio(false); cin.tie(0);

    // Input
    cin >> M >> N;

}